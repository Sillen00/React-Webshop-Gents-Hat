import { FormControl, FormControlLabel, FormLabel, Radio, RadioGroup } from '@mui/material'
import { Product } from '../../data'

interface Props {
  product?: Product
}

export function RadioButton({ product }: Props) {
  return (
    <>
    </>    
  )
}
